TEMPLATES={
    load:(LANGUAGE,toolbox)=>{
          
        toolbox.addDatabase("databases/base");
        toolbox.addDatabase("databases/extras");
        toolbox.addDatabase("databases/custom");

    }
}